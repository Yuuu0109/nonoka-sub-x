Subject: [PATCH 6/6] Declare the video downloader's optional tools

Reason: The desktop video downloader needs tools upstream's manifest does not
declare, and all of them are optional: without them the downloader still runs,
it is just slower and fails on anything but short videos. Finoka has no resource
system of its own -- every managed binary it can find lives under
`finesub/runtime/<id>/`, resolved by `managedtools` -- so they have to be
declared here to be installable at all.

  aria2c         Multi-connection fetch. Pure speed; the built-in downloader
                 works without it.
  node           A JavaScript runtime. yt-dlp needs one to solve the "n"
                 challenge (it enables only deno by default), and the PO token
                 generator runs on it.
  pot-provider   Proof-of-origin tokens. YouTube only returns usable formats to
                 a signed-in session whose request carries one, and yt-dlp
                 cannot mint them itself.

They are declared together because they only work together: YouTube gates in
sequence, and a missing piece fails at an earlier gate than the one it belongs
to, which is what makes each look useless when added alone.

`pot-provider` carries both halves of the provider -- the yt-dlp plugin and the
generator it calls. Upstream ships no built backend (it needs an npm install
with a native `canvas`, which is not a single archive), and versions the two
halves together, so Finoka builds and publishes one bundle instead: see
`scripts/build_pot_bundle.py`. It is GPL-3.0, and the bundle carries the licence
and corresponding source accordingly.

Every asset is pinned by size and sha256. aria2c's binary and node's sit one
directory down inside their archives, so `required_files` names those paths.

Removal: Delete once upstream declares these tools in its own manifest.

diff --git a/resources/runtime-manifest.json b/resources/runtime-manifest.json
index f9c1836..702ab08 100644
--- a/resources/runtime-manifest.json
+++ b/resources/runtime-manifest.json
@@ -90,6 +90,52 @@
         "size": 9303247,
         "sha256": "4e1d215bec6f2f48f8271673a1339d289bf8650a155b88a6210174bd133e6eec"
       }
+    },
+    {
+      "id": "aria2c",
+      "version": "1.37.0",
+      "destination": "runtime",
+      "directory": "aria2c",
+      "archive_type": "zip",
+      "required_files": [
+        "aria2-1.37.0-win-64bit-build1/aria2c.exe"
+      ],
+      "asset": {
+        "url": "https://github.com/aria2/aria2/releases/download/release-1.37.0/aria2-1.37.0-win-64bit-build1.zip",
+        "size": 2475379,
+        "sha256": "67d015301eef0b612191212d564c5bb0a14b5b9c4796b76454276a4d28d9b288"
+      }
+    },
+    {
+      "id": "node",
+      "version": "24.20.0",
+      "destination": "runtime",
+      "directory": "node",
+      "archive_type": "zip",
+      "required_files": [
+        "node-v24.20.0-win-x64/node.exe"
+      ],
+      "asset": {
+        "url": "https://nodejs.org/dist/v24.20.0/node-v24.20.0-win-x64.zip",
+        "size": 37539751,
+        "sha256": "6cac9ffbca8f6a47091e4b5c772e0606049c3871cb67d900c0cedde630e545ba"
+      }
+    },
+    {
+      "id": "pot-provider",
+      "version": "1.3.2",
+      "destination": "runtime",
+      "directory": "pot-provider",
+      "archive_type": "zip",
+      "required_files": [
+        "build/generate_once.js",
+        "yt_dlp_plugins/extractor/getpot_bgutil_http.py"
+      ],
+      "asset": {
+        "url": "https://github.com/Ricori/finoka/releases/download/pot-provider/pot-provider-1.3.2-windows-amd64.zip",
+        "size": 28601990,
+        "sha256": "5c269e87efb307a8ca034417af8e620ddc30ee3109b9231e684351561214d70e"
+      }
     }
   ]
 }
